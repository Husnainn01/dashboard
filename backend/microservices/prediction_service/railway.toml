[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python prediction_service.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
port = 5003
